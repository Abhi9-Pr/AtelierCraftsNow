/**
 * The hero card's rendered width, as a `sizes` value. Shared by the card, so
 * the browser picks the right image variant, and by the build, which preloads
 * the same variant in the home page's HTML. The two must stay identical.
 */
export const HERO_IMAGE_SIZES = '(min-width: 48rem) 16rem, 14rem'
